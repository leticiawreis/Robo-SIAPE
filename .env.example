# Nenhuma variável de ambiente é obrigatória para rodar o projeto atualmente
# (o download é público e não exige credenciais de API).
#
# Este arquivo serve como modelo para configurações locais futuras.
# Copie para ".env" (sem o ".example") e preencha se/quando necessário.
# O ".env" nunca deve ser versionado — já está no .gitignore.
